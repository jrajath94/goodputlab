---
phase: "01-topologies-topo"
plan: "07"
type: "execute"
wave: 3
depends_on:
  - "01-03"
  - "01-04"
files_modified:
  - "README.md"
  - "tests/test_topos.py"
  - "tests/test_schema_uniformity.py"
autonomous: true
requirements:
  - "TOPO-01"
  - "TOPO-02"
  - "TOPO-03"
  - "TOPO-04"
  - "TOPO-07"
verification_refs:
  - id: "phase-roadmap-goal-2026-07-08"
    source: ".planning/ROADMAP.md Phase 1"
    fact: "Phase 1 succeeds when all four topologies deploy through docker-compose and serve OpenAI-compatible HTTP end-to-end."
  - id: "phase-context-common-schema-2026-07-08"
    source: ".planning/phases/01-topologies-topo/01-CONTEXT.md"
    fact: "All four topologies must expose identical OpenAI-compatible schema: /v1/chat/completions, /v1/completions, /v1/models, /health, and /metrics."
  - id: "measurement-status"
    source: ".planning/RUNPOD.md"
    fact: "Measured results, cold-start time, and topology pass/fail evidence are [NOT YET MEASURED] until execution on RunPod pod t3son251d5gcvg."
must_haves:
  truths:
    - "User can read the README and run the exact Phase 1 quickstart commands without guessing topology names or ports."
    - "Integration tests exercise the same OpenAI-compatible schema across all four topologies."
    - "README contains measured-result placeholders marked [NOT YET MEASURED], not fabricated numbers."
  artifacts:
    - path: "README.md"
      provides: "One-command quickstart, topology table, and measured-result placeholder table"
      contains: "[NOT YET MEASURED]"
    - path: "tests/test_topos.py"
      provides: "Runtime smoke tests for four topology chat endpoints"
      contains: "test_topology_chat_completion"
    - path: "tests/test_schema_uniformity.py"
      provides: "Runtime schema uniformity tests for shared endpoints"
      contains: "test_topology_exposes_common_endpoints"
  key_links:
    - from: "README.md"
      to: "Makefile"
      via: "quickstart command examples"
      pattern: "make provision"
    - from: "tests/test_topos.py"
      to: "docker-compose.yml ports"
      via: "topology base URLs"
      pattern: "18000|18001|19100|19200"
    - from: "tests/test_schema_uniformity.py"
      to: "common OpenAI-compatible endpoints"
      via: "parameterized endpoint checks"
      pattern: "/v1/chat/completions"
---

<objective>
Create the user-facing quickstart and runtime integration tests that prove the four topology endpoint contract.

Purpose: Phase 1 must end with a reproducible artifact trail: commands, ports, expected endpoints, and tests that future load generation can reuse without special-casing topology internals.

Output: README Phase 1 quickstart plus `tests/test_topos.py` and `tests/test_schema_uniformity.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/rj/Anthropic_research/workspace/goodputlab/CLAUDE.md
@/Users/rj/Anthropic_research/workspace/goodputlab/README.md
@/Users/rj/Anthropic_research/workspace/goodputlab/.planning/ROADMAP.md
@/Users/rj/Anthropic_research/workspace/goodputlab/.planning/REQUIREMENTS.md
@/Users/rj/Anthropic_research/workspace/goodputlab/.planning/phases/01-topologies-topo/01-CONTEXT.md
@/Users/rj/Anthropic_research/workspace/goodputlab/.planning/phases/01-topologies-topo/01-RESEARCH.md
@/Users/rj/Anthropic_research/workspace/goodputlab/.planning/RUNPOD.md

<decision_trace>
D-04 Compose Layout: README and tests document one compose file with four profiles.
D-05 Common Endpoint Contract: tests assert `/v1/chat/completions`, `/v1/completions`, `/v1/models`, `/health`, and `/metrics` for every topology.
Integrity Baseline: measured values must be marked `[NOT YET MEASURED]` until the pod run produces evidence.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime topology smoke tests</name>
  <read_first>
    @/Users/rj/Anthropic_research/workspace/goodputlab/tests/test_topos.py (create if missing)
    @/Users/rj/Anthropic_research/workspace/goodputlab/docker-compose.yml
    @/Users/rj/Anthropic_research/workspace/goodputlab/.planning/phases/01-topologies-topo/01-CONTEXT.md
  </read_first>
  <files>tests/test_topos.py</files>
  <behavior>
    - Parametrize topologies `colocated`, `chunked`, `disagg`, and `disagg-tier` against base URLs `http://localhost:18000/v1`, `http://localhost:18001/v1`, `http://localhost:19100/v1`, and `http://localhost:19200/v1`.
    - For each topology, POST `/chat/completions` with `model=goodputlab-model`, deterministic short user message, `temperature=0.0`, and `max_tokens=8`.
    - Skip cleanly with a clear message when the topology endpoint is not running; fail on malformed successful responses.
  </behavior>
  <action>Create `tests/test_topos.py` with pytest tests for runtime topology smoke coverage. Include an environment variable gate `GOODPUTLAB_RUN_LIVE=1` so local `pytest` does not fail when the RunPod pod is stopped; when the gate is absent, tests should skip with a message naming the required make target. When the gate is set, require HTTP 200 from `/v1/chat/completions`, response JSON field `choices`, and non-empty generated text or message content. Use `requests` with explicit timeouts and no API key. Keep the served model name exactly `goodputlab-model` per D-05.</action>
  <verify>
    <automated>python3 -m compileall tests/test_topos.py && GOODPUTLAB_RUN_LIVE=0 python3 -m pytest tests/test_topos.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_topos.py` contains exact topology names `colocated`, `chunked`, `disagg`, and `disagg-tier`.
    - `tests/test_topos.py` contains exact base URLs for ports `18000`, `18001`, `19100`, and `19200`.
    - `tests/test_topos.py` contains exact string `GOODPUTLAB_RUN_LIVE`.
    - `tests/test_topos.py` sends `model` value `goodputlab-model`.
  </acceptance_criteria>
  <done>Live topology smoke tests exist and are safe to run when the pod is stopped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add schema uniformity tests for common endpoint contract</name>
  <read_first>
    @/Users/rj/Anthropic_research/workspace/goodputlab/tests/test_schema_uniformity.py (create if missing)
    @/Users/rj/Anthropic_research/workspace/goodputlab/tests/test_topos.py
    @/Users/rj/Anthropic_research/workspace/goodputlab/docker-compose.yml
    @/Users/rj/Anthropic_research/workspace/goodputlab/.planning/phases/01-topologies-topo/01-CONTEXT.md
  </read_first>
  <files>tests/test_schema_uniformity.py</files>
  <behavior>
    - For every topology, check `/health`, `/v1/models`, and `/metrics`.
    - Assert `/v1/models` returns JSON with `data` containing model id `goodputlab-model`.
    - Assert `/metrics` response contains vLLM metric text for vLLM-backed services or proxy-combined vLLM metrics for disagg services.
  </behavior>
  <action>Create `tests/test_schema_uniformity.py` with pytest parameterization over the same four topology names and base ports. Use the same `GOODPUTLAB_RUN_LIVE=1` gate. When live, assert `/health` returns 200, `/v1/models` returns a JSON object containing `data` and `goodputlab-model`, `/metrics` returns text containing `vllm:` or the proxy's prefill/decode metric sections, and OpenAI path strings are tested through the chat smoke in `tests/test_topos.py`. Do not invent benchmark numbers or assert latency thresholds here; those remain [NOT YET MEASURED].</action>
  <verify>
    <automated>python3 -m compileall tests/test_schema_uniformity.py && GOODPUTLAB_RUN_LIVE=0 python3 -m pytest tests/test_schema_uniformity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_schema_uniformity.py` contains endpoint strings `/health`, `/v1/models`, and `/metrics`.
    - `tests/test_schema_uniformity.py` contains exact model id `goodputlab-model`.
    - `tests/test_schema_uniformity.py` contains exact string `[NOT YET MEASURED]` only in explanatory comments or assertion messages, not as a passing measured value.
    - Tests skip when `GOODPUTLAB_RUN_LIVE` is not set to `1` and run live when it is set to `1`.
  </acceptance_criteria>
  <done>Common endpoint contract is executable as a live test suite without requiring the pod for default local runs.</done>
</task>

<task type="auto">
  <name>Task 3: Update README with Phase 1 quickstart and evidence placeholders</name>
  <read_first>
    @/Users/rj/Anthropic_research/workspace/goodputlab/README.md
    @/Users/rj/Anthropic_research/workspace/goodputlab/Makefile
    @/Users/rj/Anthropic_research/workspace/goodputlab/.planning/RUNPOD.md
    @/Users/rj/Anthropic_research/workspace/goodputlab/.planning/phases/01-topologies-topo/01-CONTEXT.md
  </read_first>
  <files>README.md</files>
  <action>Update README in plain technical prose with: one-sentence project description; current Phase 1 status; requirements section naming Python 3.11, Docker Compose v2, NVIDIA runtime, RunPod H100 NVL pod `t3son251d5gcvg`; quickstart commands `make install-dev`, `make provision`, `make up-colocated`, `make up-chunked`, `make up-disagg`, `make up-disagg-tier`, `make health`; topology table with profiles, make targets, external ports, and endpoint base URLs; a measured-results table where cold-start time, sentinel pass count, TTFT, ITL, and cost are all exactly `[NOT YET MEASURED]`; a safety section explaining UCX-only NIXL and sentinel-token validation; and limitations noting single-node, single-tenant, no benchmark claims yet. Remove marketing language and any emoji from existing README content.</action>
  <verify>
    <automated>grep -q 'make provision' README.md && grep -q '\[NOT YET MEASURED\]' README.md && grep -q 'UCX-only' README.md && ! grep -q '🏗️' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `make provision`, `make health`, `make up-colocated`, `make up-chunked`, `make up-disagg`, and `make up-disagg-tier`.
    - `README.md` contains ports `18000`, `18001`, `19100`, and `19200`.
    - `README.md` contains a measured-results table with `[NOT YET MEASURED]` placeholders.
    - `README.md` contains `UCX-only` and `sentinel-token`.
    - `README.md` does not contain emoji or fabricated benchmark values.
  </acceptance_criteria>
  <done>README gives exact Phase 1 operating instructions and preserves measurement integrity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest to live topology endpoints | Optional live tests send requests to local topology ports when explicitly enabled. |
| README to human operator | README commands may cause GPU startup and RunPod billing when executed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-S | Spoofing | endpoint identity in tests | mitigate | Tests assert `goodputlab-model` from `/v1/models` before accepting endpoint readiness. |
| T-01-07-R | Repudiation | README evidence claims | mitigate | Mark every unmeasured value `[NOT YET MEASURED]` until logs/results exist. |
| T-01-07-I | Information Disclosure | README and tests | mitigate | Do not document secrets, tokens, or private env values; tests require no API key. |
| T-01-07-D | Denial of Service | default pytest behavior | mitigate | Live tests are gated by `GOODPUTLAB_RUN_LIVE=1` so default local tests do not hit stopped pod endpoints. |
| T-01-SC | Tampering | test dependencies | mitigate | Use requests and pytest from pyproject; no new package installs in this plan. |
</threat_model>

<verification>
Run `GOODPUTLAB_RUN_LIVE=0 python3 -m pytest tests/test_topos.py tests/test_schema_uniformity.py -q` locally. On the pod after all profiles are up, run `GOODPUTLAB_RUN_LIVE=1 python3 -m pytest tests/test_topos.py tests/test_schema_uniformity.py -q`.
</verification>

<success_criteria>
- README contains exact quickstart commands and honest `[NOT YET MEASURED]` placeholders.
- Integration tests cover all four topology endpoints and the common schema contract.
- Default local tests skip live endpoint checks unless explicitly enabled.
- No deferred Phase 5/6/7/8 functionality is implemented.
</success_criteria>

<output>
Create `/Users/rj/Anthropic_research/workspace/goodputlab/.planning/phases/01-topologies-topo/01-07-SUMMARY.md` when done.
</output>
