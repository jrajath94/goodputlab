---
phase: "01-topologies-topo"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "pyproject.toml"
  - "Makefile"
  - ".gitignore"
  - "core/__init__.py"
  - "control/__init__.py"
  - "tests/conftest.py"
  - "tests/test_project_skeleton.py"
autonomous: true
requirements:
  - "TOPO-07"
verification_refs:
  - id: "ctx7-vllm-nixl-2026-07-08"
    source: "Context7 /websites/vllm_ai_en_stable, vLLM NixlConnector and disagg prefill docs"
    fact: "NixlConnector kv-transfer-config accepts kv_connector, kv_role, kv_buffer_device, and kv_connector_extra_config.backends; roles include kv_producer and kv_consumer."
  - id: "hf-qwen-official-2026-07-08"
    source: "Hugging Face API author=Qwen search=Qwen3.6"
    fact: "Official Qwen3.6 FP8 candidates include Qwen/Qwen3.6-35B-A3B-FP8 and Qwen/Qwen3.6-27B-FP8; plans use the FP8 family per D-01 with runtime support probing."
  - id: "ctx7-vllm-chunked-kv-2026-07-08"
    source: "Context7 /websites/vllm_ai_en_stable, vLLM scheduler/config docs"
    fact: "vLLM docs show enable_chunked_prefill and kv_transfer_config as independent scheduler/config fields except connector-specific incompatibilities; Phase 1 keeps chunked and kv-transfer flags separated per topology."
  - id: "measurement-status"
    source: ".planning/RUNPOD.md"
    fact: "RunPod pod t3son251d5gcvg is STOPPED; cold-start, health, and latency values remain [NOT YET MEASURED] until execution."
must_haves:
  truths:
    - "Developer can run project quality commands through Makefile targets without remembering tool flags."
    - "All topology plans share the same served-model and OpenAI-compatible endpoint contract."
    - "Python tooling enforces Python 3.11+, ruff, mypy, and pytest conventions before GPU work starts."
  artifacts:
    - path: "pyproject.toml"
      provides: "Python project metadata plus ruff, mypy, and pytest configuration"
      contains: "requires-python = \">=3.11\""
    - path: "Makefile"
      provides: "Primary command interface for provision, topology startup, health, lint, and tests"
      contains: "up-disagg-tier"
    - path: "tests/test_project_skeleton.py"
      provides: "Fast local smoke test for importable core/control packages"
      contains: "test_core_and_control_importable"
  key_links:
    - from: "Makefile"
      to: "scripts/health.sh"
      via: "health target"
      pattern: "bash scripts/health.sh all"
    - from: "pyproject.toml"
      to: "tests/"
      via: "pytest configuration"
      pattern: "testpaths = [\"tests\"]"
---

<objective>
Create the local project skeleton and command surface that every Phase 1 topology plan builds on.

Purpose: Phase 1 must be reproducible and lintable before any GPU time is spent. This plan establishes the Python 3.11, ruff, mypy, pytest, coverage, Makefile, and secret-ignore contracts used by later plans.

Output: pyproject.toml, Makefile, package skeletons, root test scaffolding, and .gitignore hardening.
</objective>

<execution_context>
@/Users/rj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/rj/Anthropic_research/workspace/goodputlab/CLAUDE.md
@/Users/rj/Anthropic_research/workspace/goodputlab/.planning/PROJECT.md
@/Users/rj/Anthropic_research/workspace/goodputlab/.planning/ROADMAP.md
@/Users/rj/Anthropic_research/workspace/goodputlab/.planning/STATE.md
@/Users/rj/Anthropic_research/workspace/goodputlab/.planning/REQUIREMENTS.md
@/Users/rj/Anthropic_research/workspace/goodputlab/.planning/phases/01-topologies-topo/01-CONTEXT.md
@/Users/rj/Anthropic_research/workspace/goodputlab/.planning/phases/01-topologies-topo/01-RESEARCH.md

<decision_trace>
D-01 Model Selection: Qwen 3.6 latest stable, FP8 on H100 NVL; runtime support probing may select a documented Qwen3.x fallback only when the pinned vLLM image cannot load the Qwen3.6 architecture.
D-02 NIXL Backend: UCX only through kv-transfer-config backends=["UCX"]; LIBFABRIC excluded.
D-03 Sentinel Defense: standalone tests/sentinel.py, scripts/health.sh integration, and scripts/sentinel_daemon.py emitting sentinel_drift.
D-04 Compose Layout: one docker-compose.yml with profiles colocated, chunked, disagg, disagg-tier.
D-05 Common Endpoint Contract: every topology exposes /v1/chat/completions, /v1/completions, /v1/models, /health, and /metrics using served model name goodputlab-model.
</decision_trace>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python project and importable package skeleton</name>
  <read_first>
    @/Users/rj/Anthropic_research/workspace/goodputlab/pyproject.toml (create if missing)
    @/Users/rj/Anthropic_research/workspace/goodputlab/core/__init__.py (create if missing)
    @/Users/rj/Anthropic_research/workspace/goodputlab/control/__init__.py (create if missing)
    @/Users/rj/Anthropic_research/workspace/goodputlab/tests/conftest.py (create if missing)
    @/Users/rj/Anthropic_research/workspace/goodputlab/tests/test_project_skeleton.py (create if missing)
    @/Users/rj/Anthropic_research/workspace/goodputlab/CLAUDE.md
    @/Users/rj/Anthropic_research/workspace/goodputlab/.planning/REQUIREMENTS.md
  </read_first>
  <files>pyproject.toml, core/__init__.py, control/__init__.py, tests/conftest.py, tests/test_project_skeleton.py</files>
  <action>Create `pyproject.toml` for package `goodputlab` with Python `>=3.11`, Pydantic v2, FastAPI 0.115+, uvicorn, requests, httpx, pytest, pytest-cov, ruff, mypy, types-requests, and prometheus-client declared in the appropriate dependency groups. Configure ruff target `py311`, line length `100`, and selected rules `E`, `F`, `I`, `UP`, `B`, `SIM`. Configure mypy with `python_version = "3.11"`, `strict = true`, and explicit package roots `core`, `control`, `scripts`, and `tests`. Configure pytest with `testpaths = ["tests"]` and coverage commands targeting `core` and `control` so the Phase 8 coverage and lint gates are enforceable from this commit onward (these are enforced by Phase 8, not Phase 1). Create importable `core` and `control` packages and a fast `tests/test_project_skeleton.py` that imports both packages. This sets the common Python contract used by later topology, proxy, sentinel, and health plans.</action>
  <verify>
    <automated>python3 -c "import tomllib; tomllib.load(open('pyproject.toml','rb')); print('pyproject ok')" && python3 -m compileall core control tests</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `requires-python = ">=3.11"`.
    - `pyproject.toml` contains `[tool.ruff]`, `[tool.mypy]`, and `[tool.pytest.ini_options]` sections.
    - `tests/test_project_skeleton.py` contains `test_core_and_control_importable`.
    - `core/__init__.py` and `control/__init__.py` exist and are importable by Python 3.11.
  </acceptance_criteria>
  <done>Python tooling configuration, web proxy dependencies, and importable package skeleton exist; local static parsing and byte-compilation succeed.</done>
</task>

<task type="auto">
  <name>Task 2: Create Makefile command contract and harden ignored secrets</name>
  <read_first>
    @/Users/rj/Anthropic_research/workspace/goodputlab/Makefile (create if missing)
    @/Users/rj/Anthropic_research/workspace/goodputlab/.gitignore
    @/Users/rj/Anthropic_research/workspace/goodputlab/.planning/phases/01-topologies-topo/01-CONTEXT.md
    @/Users/rj/Anthropic_research/workspace/goodputlab/.planning/phases/01-topologies-topo/01-RESEARCH.md
  </read_first>
  <files>Makefile, .gitignore</files>
  <action>Create a Makefile with explicit phony targets `install-dev`, `lint`, `test`, `compose-config`, `provision`, `up-colocated`, `up-chunked`, `up-disagg`, `up-disagg-tier`, `down`, `health`, and `sentinel`. The topology targets must call `docker compose --profile colocated up -d`, `docker compose --profile chunked up -d`, `docker compose --profile disagg up -d`, and `docker compose --profile disagg-tier up -d` per D-04. The `health` target must call `bash scripts/health.sh all`; the `sentinel` target must call `python3 tests/sentinel.py --mode check --base-url http://localhost:19100/v1`. The `lint` target must run `ruff check .` and `mypy .`; the `test` target must run pytest with coverage for `core` and `control`. Update `.gitignore` to include `.env`, `openclaw.json`, `whoop-token.json`, `discord_bot_config.json`, `terraform.tfvars`, generated sentinel fixture JSON under `tests/_fixtures/*.json`, generated runtime env files under `configs/runtime*.env`, logs, caches, and benchmark result artifacts while preserving `bench/results/.gitkeep`.</action>
  <verify>
    <automated>make -n lint && make -n test && make -n compose-config && make -n up-colocated && make -n up-chunked && make -n up-disagg && make -n up-disagg-tier && grep -v '^#' .gitignore | grep -q '^terraform.tfvars$'</automated>
  </verify>
  <acceptance_criteria>
    - `Makefile` contains phony targets for all four topology commands: `up-colocated`, `up-chunked`, `up-disagg`, and `up-disagg-tier`.
    - `Makefile` contains `compose-config` that validates all four profiles with `docker compose --profile ... config`.
    - `.gitignore` contains exact lines `.env`, `openclaw.json`, `whoop-token.json`, `discord_bot_config.json`, and `terraform.tfvars`.
    - `make -n health` prints a command containing `bash scripts/health.sh all`.
  </acceptance_criteria>
  <done>Project command surface exists and protected secret filenames cannot be committed accidentally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell to local tooling | Makefile invokes lint, tests, Docker, and shell scripts from the repository root. |
| package metadata to Python environment | pyproject dependency declarations influence any later local install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-T | Tampering | `.gitignore` and generated runtime files | mitigate | Ignore `.env`, RunPod/runtime env files, sentinel fixture outputs, logs, and known private config names so secrets and generated measurements are not committed. |
| T-01-01-R | Repudiation | Makefile commands | mitigate | Provide explicit Makefile targets so executor logs show the exact command used for lint, test, compose validation, provision, health, and topology startup. |
| T-01-01-D | Denial of Service | lint/test commands | accept | Local lint and test commands are developer-only and run on the workstation, not public endpoints. |
| T-01-SC | Tampering | PyPI and Docker package supply chain | mitigate | Use packages from the Phase 1 research package audit and standard dev tooling; no package install is executed by this plan. Later install/provision plans must verify image and package versions before running GPU work. |
</threat_model>

<verification>
Run `make -n lint`, `make -n test`, `make -n compose-config`, and `python3 -m compileall core control tests`. Full lint/test execution occurs after dependencies are installed through `make install-dev`.
</verification>

<success_criteria>
- `pyproject.toml` parses with `tomllib` and declares Python 3.11+, ruff, mypy, pytest, and coverage configuration.
- `Makefile` exposes the exact phase command contract used by every later plan.
- `.gitignore` protects all user-forbidden secret files.
- No Phase 1 deferred ideas are implemented.
</success_criteria>

<output>
Create `/Users/rj/Anthropic_research/workspace/goodputlab/.planning/phases/01-topologies-topo/01-01-SUMMARY.md` when done.
</output>
